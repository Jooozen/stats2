# バスケスタッツ - 使い方マニュアル

## このアプリの特徴

### 1. 完全オフライン動作
インターネット接続不要。データはすべてデバイス内（IndexedDB）に保存されるため、体育館の電波が悪い環境でも問題なく使えます。

### 2. リアルタイムスタッツ記録
試合中にワンタップでスタッツを記録。2P/3P/FT/REB/AST/STL/BLK/TO/FOUL の全12種のアクションをすばやく入力できます。

### 3. コート図によるシュートゾーン記録
SVGで描画されたバスケットコート上の9ゾーンをタップし、成功/失敗を記録。選手ごとにエリア別のシュート成功率を蓄積・分析できます。

### 4. 紅白戦（同一チーム対戦）対応
同じチーム同士の練習試合にも対応。A側/B側に選手を振り分け、スコア・スタッツを完全に分離して記録できます。

### 5. 柔軟なタイマー管理
カウントダウンタイマーは一時停止・手入力による時刻修正が可能。ページを離れても状態を保持し、再開時に正確に復元されます。

### 6. カテゴリ別ルール自動設定
中学（8分Q）、高校（10分Q）、社会人（10分Q）から選ぶだけで、クォーター時間・タイムアウト回数が自動設定されます。

### 7. CSV エクスポート
試合終了後、選手別スタッツとタイムライン（プレイログ）の2種類のCSVをダウンロード可能。Excel等での詳細分析に活用できます。

### 8. ワークスペース分離
複数人が同じデバイスを使っても、ワークスペース（アカウント）ごとにデータが完全に分離されます。

---

## 使い方ガイド（順序通り）

---

### STEP 1: ワークスペースの作成（初回のみ）

1. アプリを開くとログイン画面が表示されます
2. **「新規作成」** タブを選択
3. **ワークスペース名**（例: 自分の名前やチーム名）を入力
4. **パスコード**（4文字以上）を設定
5. **「作成」** をタップ → ホーム画面に進みます

> 次回以降は「ログイン」タブからワークスペース名とパスコードを入力してください。

---

### STEP 2: チームの登録

1. 画面下部のナビゲーションから **「チーム」** をタップ
2. **チーム名** を入力し **「＋ 追加」** をタップ
3. 背番号4〜18の選手枠が自動で作成されます
4. **「選手管理」** ボタンをタップすると編集モードになります
5. 各選手の **名前** をタップして入力 → **「保存」**
6. 選手の追加・削除もここで行えます

> **ポイント**: 名前を入力しなくても背番号だけで登録されます。相手チームなど名前がわからない場合でも背番号のみでOKです。

---

### STEP 3: 試合の作成

1. 画面下部の **「試合」** をタップ
2. **「＋ 新しい試合を作成」** ボタンをタップ
3. 以下の項目を設定します:

| 項目 | 説明 |
|------|------|
| 自チーム | ドロップダウンから登録済みチームを選択 |
| 試合タイトル | 任意（例: 練習試合 vs ○○高校） |
| 試合日 | 日付を選択 |
| カテゴリ | 中学 / 高校 / 社会人 から選択 |
| クォーター時間 | 5〜12分（カテゴリ選択で自動設定） |
| 対戦相手 | 「登録済み」または「新規入力」を選択 |

4. **「試合を作成して開始」** をタップ

> **紅白戦の場合**: 対戦相手に自チームと同じチームを選択してください。自動的にA側/B側モードになります。

> **新規相手チーム**: 「新規入力」を選ぶとチーム名と最低5人の背番号を入力するフォームが表示されます。

---

### STEP 4: スターティング5の選択

試合画面を開くと、まず **スターティング5選択パネル** が表示されます。

1. **左側（自チーム）** から出場する5名をタップして選択
2. **右側（相手チーム）** から出場する5名をタップして選択
3. 選手が足りない場合は、各側の **「番号」「名前」入力欄** からその場で追加可能
4. 両チーム5名ずつ選んだら **「確定して試合開始」** をタップ

> **紅白戦の場合**: 片方で選んだ選手はもう片方ではグレーアウトされ選択できません。

---

### STEP 5: 試合中のスタッツ記録

#### 画面構成（上から順に）

```
┌──────────────────────────────┐
│  スコアボード（スコア/Q/タイマー） │
├──────────────────────────────┤
│  選択中の選手（フィードバック表示）│
├──────────┬───────────────────┤
│ 自チーム  │  相手チーム       │
│  5名表示  │   5名表示        │
├──────────┴───────────────────┤
│    コート図（シュートゾーン）    │
├──────────────────────────────┤
│  スタッツボタン 12個（2行×6列） │
│  戻す / メンバーチェンジ / タイマー│
└──────────────────────────────┘
```

#### 基本操作の流れ

1. **選手をタップ** して選択（ハイライトされます）
2. **スタッツボタンをタップ** して記録

| ボタン | 意味 |
|--------|------|
| 2P | 2ポイント成功 |
| 3P | 3ポイント成功 |
| FT | フリースロー成功 |
| REB | リバウンド |
| AST | アシスト |
| STL | スティール |
| ミス2P | 2ポイント失敗 |
| ミス3P | 3ポイント失敗 |
| ミスFT | フリースロー失敗 |
| BLK | ブロック |
| TO | ターンオーバー |
| FOUL | ファール |

#### コート図からのシュート記録

1. 選手を選択した状態で **コート図のゾーン** をタップ
2. **「成功」** または **「失敗」** をタップ
3. 2Pゾーン/3Pゾーンが自動判定されます

#### タイマー操作

- **▶ / ⏸**: タイマーの開始/一時停止
- **RST**: タイマーを初期値にリセット
- 停止中は分・秒を直接入力して修正可能
- 残り1分を切ると赤く点滅

#### 記録の取り消し

- **「戻す」** ボタンで直前の記録を1件取り消し

---

### STEP 6: メンバーチェンジ（交代）

1. 画面下部の **「メンバーチェンジ」** をタップ
2. 上部タブで **自チーム / 相手チーム** を切り替え
3. 交代方法（どちらの順でもOK）:
   - **コート上の選手** をタップ → **ベンチの選手** をタップ → 即交代
   - **ベンチの選手** をタップ → **コート上の選手** をタップ → 即交代
4. 新しい選手を追加したい場合は下部の入力フォームから追加

---

### STEP 7: 試合中のスタッツ確認

- **「スタッツ」** ボタンをタップ → 各選手の現在のスタッツ一覧を表示
- **「タイムライン」** ボタンをタップ → 時系列のプレイログを表示
  - タイムラインからは各記録の **修正・削除** も可能

---

### STEP 8: クォーターの切り替え

- スコアボード内の **Q1〜Q4 / OT** ボタンをタップ
- OTに切り替えるとタイマーがOT用時間（5分）にリセットされます

---

### STEP 9: 試合終了

1. **「試合終了」** をタップして確認
2. 自動的にサマリーページに移動します

> **中断したい場合**: 「中断」ボタンで試合を一時保存。後から試合一覧から再開できます。

---

### STEP 10: 試合結果の確認とエクスポート

サマリーページでは以下が確認できます:

- **最終スコア**
- **チーム別スタッツ表**（PTS, FG, 3P, FT, REB, AST, STL, BLK, TO, FOUL）
- **チーム合計行**

#### CSVダウンロード

- **「スタッツCSV」**: 選手別の成績データ
- **「タイムラインCSV」**: 時系列のプレイログ

> Excel等で開いて、さらに詳細な分析が可能です。

---

### STEP 11: 選手のシュート分析（チームページ）

1. **「チーム」** ページで対象チームを展開
2. 選手の **「詳細」** をタップ
3. エリア別シュート率が表示されます:
   - 全エリア合計の成功率
   - 9ゾーン個別の成功/失敗/試投/成功率
   - 50%以上=緑、30%以上=黄、30%未満=赤で色分け

> このデータは全試合の累積です。練習や試合を重ねるほど精度の高い分析が可能になります。

---

## よくある質問

**Q: データはクラウドに保存されますか？**
A: いいえ。すべてデバイスのブラウザ内（IndexedDB）に保存されます。ブラウザのデータを消去するとデータも消えるのでご注意ください。

**Q: 複数デバイスでデータを共有できますか？**
A: 現在はデバイス間の同期機能はありません。各デバイスで独立して動作します。

**Q: 試合中にアプリを閉じてしまったら？**
A: タイマーの状態やスタッツはすべてDBに保存されています。再度アプリを開き、試合一覧から該当試合をタップすれば続きから記録できます。

**Q: 相手チームの選手名がわからない場合は？**
A: 背番号のみで登録・選択が可能です。名前は空欄のままでOKです。
